package Comparisons;

public class Operators {
    //Have to create a variable to hold total without modifying x and y
    int x = 1;
    int y = 2;
    int z;

    z = x + y;

    z = x - y;

    z = x * y;

    z = x / y;

    if (x < y)
        break;
    if (x > y)
        break;
    if (x <= y)
        break;
    if (x >= y)
        break;
    if (x == y)
        break;
}