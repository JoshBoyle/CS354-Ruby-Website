int main() {
    int x = 1;
    int y = 2;
    int z;

    z = x + y;

    z = x - y;

    z = x * y;

    z = x / y;

    if(x < y)
        return;
    if(x > y)
        return;
    if(x <= y)
        return;
    if(x >= y)
        return;
    if(x == y)
        return;

}